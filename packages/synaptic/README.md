# synaptic

This library was generated with [Nx](https://nx.dev).

## Building

Run `nx build synaptic` to build the library.

## Running unit tests

Run `nx test synaptic` to execute the unit tests via [Jest](https://jestjs.io).
