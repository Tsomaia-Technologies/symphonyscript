# legacy

This library was generated with [Nx](https://nx.dev).

## Building

Run `nx build legacy` to build the library.

## Running unit tests

Run `nx test legacy` to execute the unit tests via [Jest](https://jestjs.io).
